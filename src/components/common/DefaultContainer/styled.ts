import { NAV_HEIGHT, NAV_WIDTH } from '@shared/constants';
import styled from 'styled-components';

export const DefaultContainer = styled.div`
  max-width: ${NAV_WIDTH}rem;
  margin: ${NAV_HEIGHT}rem auto 0;
`;
